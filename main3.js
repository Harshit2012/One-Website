function open_agecw(){
    window.location = "Age Calculator/index.html"
}

function open_analogcw(){
    window.location = "Analog Clock/index.html"
}

function open_cheatsheetw(){
    window.location = "CheatSheet/index.html"
}

function open_contactfw(){
    window.location = "Contact Form/index.html"
}

function open_currencyfw(){
    window.location = "Currency Converter Website/index.html"
}

function open_digitalcw(){
    window.location = "Digital Clock/index.html"
}

function open_musicalw(){
    window.location = "Musical App/index.html"
}

function open_passwordscw(){
    window.location = "Password Strength Checker/index.html"
}

function open_qrcgw(){
    window.location = "Qr Code Generator/index.html"
}

function open_quiz(){
    window.location = "Quiz/index.html"
}

function open_quotegw(){
    window.location = "Quote Generator/index.html"
}

function open_randompgw(){
    window.location = "Random Password Generator/index.html"
}

function open_voicettcw(){
    window.location = "Voice to Text Converter/index.html"
}

function open_weatherww(){
    window.location = "Weather Website/index.html"
}

function back(){
    window.location = "index.html"
}

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic2');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic3');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic4');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic5');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic6');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic7');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic8');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic9');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic10');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic11');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic12');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic13');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

window.addEventListener('scroll', () => {
    const text = document.querySelector('.topic14');
    const textPosition = text.getBoundingClientRect().top;
    const screenPosition = window.innerHeight;
    if(textPosition < screenPosition){
      text.classList.add('visible');
    } else {
      text.classList.remove('visible');
    }
});

$(window).load(function() {
  $(".se-pre-con").fadeOut("slow");;
});
